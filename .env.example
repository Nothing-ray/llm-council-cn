# ==============================================================================
# LLM Council 环境变量配置
# ==============================================================================

# ------------------------------------------------------------------------------
# Provider API Keys
# ------------------------------------------------------------------------------

# OpenRouter API Key
# 获取方式：访问 https://openrouter.ai/ 注册并获取 API Key
OPENROUTER_API_KEY=sk-or-your-key-here

# SiliconFlow API Key
# 获取方式：访问 https://siliconflow.cn/ 注册并获取 API Key
SILICONFLOW_API_KEY=your-siliconflow-key-here

# ------------------------------------------------------------------------------
# Provider Selection (可选)
# ------------------------------------------------------------------------------

# 选择激活的 provider (可选)
# 如果不设置，将使用 config/config.json 中的 active_provider 配置
# 可选值: openrouter | siliconflow
# LLM_COUNCIL_ACTIVE_PROVIDER=siliconflow

# ------------------------------------------------------------------------------
# 其他配置 (可选)
# ------------------------------------------------------------------------------

# 配置文件路径 (可选)
# 如果不设置，将依次尝试以下位置：
# 1. 环境变量 LLM_COUNCIL_CONFIG_PATH 指定的路径
# 2. 项目根目录的 config.local.json
# 3. 项目根目录的 config.json
# LLM_COUNCIL_CONFIG_PATH=/path/to/custom/config.json
